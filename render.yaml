services:
  - type: web
    name: sanisostenible
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 10000
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
    healthCheckPath: /
    autoDeploy: true
